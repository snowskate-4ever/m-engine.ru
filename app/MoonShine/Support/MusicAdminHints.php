<?php

declare(strict_types=1);

namespace App\MoonShine\Support;

/**
 * Подсказки для полей вёрстки публичных страниц (JSON из кабинета пользователя).
 */
final class MusicAdminHints
{
    public const LAYOUT_DRAFT = 'Структура блоков до публикации. Обычно правится в пользовательском кабинете (Livewire). '
        .'Формат: массив с ключами version и blocks (список {id, enabled, order}). '
        .'Некорректный JSON может сломать отображение на сайте — правьте только при необходимости поддержки.';

    public const LAYOUT_PUBLISHED = 'То же, что черновик, но эта версия показывается гостям при включённой публикации и заполненном slug.';

    public const SHOP_ORDER_FORM = 'Заказы создаются оформлением из кабинета; здесь обычно меняют только статус. Магазин, покупатель и примечание — только для чтения.';
}
