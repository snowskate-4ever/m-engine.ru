<?php

declare(strict_types=1);

return [

    /*
    |--------------------------------------------------------------------------
    | Комиссия платформы (доля от суммы в minor units после успешной оплаты)
    |--------------------------------------------------------------------------
    */
    'platform_fee_bps' => (int) env('PLATFORM_FEE_BPS', 500), // 5% = 500 basis points

    /*
    |--------------------------------------------------------------------------
    | Эскроу: удержание средств до подтверждения услуги (по типу payable)
    |--------------------------------------------------------------------------
    | Ключи: booking, event, lesson — при отсутствии ключа = false
    */
    'escrow_enabled_for' => [
        'booking' => (bool) env('PLATFORM_ESCROW_BOOKING', false),
        'event' => (bool) env('PLATFORM_ESCROW_EVENT', false),
    ],

    /*
    |--------------------------------------------------------------------------
    | Частичный возврат: доля при отмене до N часов до начала (по умолчанию)
    |--------------------------------------------------------------------------
    */
    'refund' => [
        'hours_before_full_refund' => (int) env('PLATFORM_REFUND_FULL_HOURS', 48),
        'partial_refund_ratio' => (float) env('PLATFORM_REFUND_PARTIAL_RATIO', 0.5),
    ],

    /*
    |--------------------------------------------------------------------------
    | Драйвер эквайринга для платформенных платежей (отдельно от AI billing)
    |--------------------------------------------------------------------------
    */
    'driver' => env('PLATFORM_PAYMENT_DRIVER', 'stub'),

];
